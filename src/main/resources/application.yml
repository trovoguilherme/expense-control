
server:
  port: 8080

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/gastos
    username: root
    password:
  jpa:
    show-sql: true
    properties:
      hibernate:
        format_sql: true
        hbm2ddl:
          import_files: sql/populate-database.sql
    hibernate:
      ddl-auto: create

# Default path: /swagger-ui/index.html
springdoc:
  swagger-ui.path: /swagger
  show-actuator: true
